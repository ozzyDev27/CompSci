public class StartWord
{
  public static String startWord(String str, String word)
  {
    if (str.substring(1).startsWith(word.substring(1))) {
        return str.substring(0,word.length());
    }
    return "";
  }
} 




/**
 * This class creates an instance of a Pokemon, having fields such as level and moveset.
 * @author Adam Rivard
 * @author Jamie Peacock
 * @since 2/23/2026
 */


/**
 * This method finds the greatest common denominator between the tax rate and the programmer's lucky number.
 * @param taxRate - int, the sales tax percent in your current region.
 * @param luckyNumber - int, the programmer's favorite number.
 * @return gcd - int, the greatest common denominator between the tax rate and programmer's lucky number.
 */


